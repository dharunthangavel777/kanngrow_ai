include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_is_not_empty: true

analyzer:
  errors:
    deprecated_member_use: ignore
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
